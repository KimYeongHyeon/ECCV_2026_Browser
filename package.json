{
  "name": "conference-atlas-template",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "configure": "node scripts/configure.mjs",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "preview": "node scripts/preview.mjs",
    "test": "node --test scripts/test/*.test.mjs",
    "check": "node --check scripts/build.mjs && node --check scripts/verify.mjs && node --check scripts/preview.mjs && node --check scripts/configure.mjs && node --check scripts/import_search_embeddings.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
